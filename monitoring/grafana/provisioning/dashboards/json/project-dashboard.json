{
  "title": "MyStartUp Project Service",
  "panels": [
    {
      "type": "timeseries",
      "title": "Task API latency",
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "targets": [{ "expr": "histogram_quantile(0.95, sum(rate(http_server_requests_seconds_bucket{application=\"project-service\"}[5m])) by (le))" }]
    }
  ],
  "schemaVersion": 39
}
